"use client";

import { useGetTickerQuery } from "@/07.shared/entities/tickers";

const MarketView = () => {
  // const { data: tickers } = useGetTickerQuery();
  // console.log(tickers);

  return <div>Market View</div>;
};

export default MarketView;
